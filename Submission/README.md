# Stroke Segmentation Challenge Submission

## Instructions

1. Place testing files (.nii.gz) into the `Input/` directory.
2. The model outputs will be generated in the `Output/` directory using same file names as inputs.
3. Environment Setup:
- Python 3.9.18
- CUDA 11.8
- cuDNN 8.7.0
4. All other environments and dependencies are listed in `requirements.txt`.
5. To run the pipeline:

```bash
python Test_script.py